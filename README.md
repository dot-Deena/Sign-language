A sign language ML model written using python. The model can currently detect four letters of ISL. Trained on 100 images each and split into train and val.
Tools used:
Google Colab,
Jupyter Notebook,
VScode,
Tensorflow,
Keras,
cvs (for real time detection).

Architecture used: Convolutional Nueral Networks.
